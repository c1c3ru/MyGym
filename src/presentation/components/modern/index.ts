/**
 * Componentes modernos reutilizáveis para UI consistente
 */

export { default as GlassCard } from '../GlassCard';
export { default as AnimatedScreen } from '../AnimatedScreen';
export { default as SectionHeader } from '../SectionHeader';
export { default as StatusBadge } from '../StatusBadge';
export { default as IconContainer } from '../IconContainer';
export { default as StudentDetailsModal } from '../StudentDetailsModal';
